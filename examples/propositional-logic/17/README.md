# 17 - Parentheses

Show that the sentence g: `Sqr(a) ∧ (Sqr(b) ∨ Sqr(c))` is not a
consequence of the sentence f: `(Sqr(a) ∧ Sqr(b)) ∨ Sqr(c)`.

You will do this by creating a counterexample
world in which f is true but g is false.
