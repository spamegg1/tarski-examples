# 14 - solution

The sixth sentence can be:

```scala
fof"∀x(Cir(x) → Sml(x))"
```

It is vacuously true in `PeirceWorld` because there are no circles there.
It is non-vacuously true in `PeanoWorld` because every circle is small there.
