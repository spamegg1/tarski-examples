# 16 - Parentheses

Show that the sentence g: `¬(Sml(a) ∨ Sml(b))` is not a
consequence of the sentence f: `¬Sml(a) ∨ Sml(b)`.

You will do this by creating a counterexample
world in which f is true but g is false.
