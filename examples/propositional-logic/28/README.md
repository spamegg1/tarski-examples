# 28 - Name that object

Open `RebusSentences` and `RebusWorld` by running `run28`.
In the world, assign names to the blocks in such a way that the sentences are all true.

## Optional: what is a rebus?

A rebus is a kind of word puzzle that uses pictures to represent words or parts of words.

<https://en.wikipedia.org/wiki/Rebus>
