# 17 - solution

You need three blocks, one for a, one for b, one for c:

```scala
val grid17: Grid = Map(
  (3, 2) -> Block(Mid, Tri, Red, "a"),
  (3, 3) -> Block(Mid, Cir, Blu, "b"),
  (3, 4) -> Block(Mid, Sqr, Lim, "c")
)
```
